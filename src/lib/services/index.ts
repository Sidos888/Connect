/**
 * Services - Independent business logic layer
 * 
 * These services work outside React component lifecycle and can be called from anywhere.
 * They follow the Service Layer Pattern used by major apps (WeChat, Facebook, Instagram).
 */

export { AuthService, authService } from './authService';
export { NavigationService, navigationService } from './navigationService';

